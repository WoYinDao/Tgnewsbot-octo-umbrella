# ============================================================
# 关注规则（可选的白名单）
# 本文件非空（有生效规则）时，只有命中至少一行的消息才会被处理；
# 保持为空（只有注释）= 不启用白名单，全部消息都处理
# ============================================================
# 语法与 blocklist.txt 相同：
#   普通词        组内“或”关系
#   +必须词       必须同时出现
#   !排除词       出现则该组不命中
# ============================================================
# 示例（去掉行首 # 即生效）：
# 只关注 AI 相关：
#   AI 人工智能 大模型 LLM
# 只关注特定公司且排除招聘贴：
#   苹果 特斯拉 !招聘
# 关注股市但必须提到 A 股：
#   股市 涨停 +A股
